name: Deploy via SSH

on:
 push:
  branches:
  - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        run: echo "Checkout repo successful"

      - name: Install sshpass
        run: echo "Install sshpass successful"

      - name: Test SSH Connection (with logs)
        run: echo "Test SSH Connection successful"
