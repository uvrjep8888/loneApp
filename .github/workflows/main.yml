name: Deploy via SSH

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Test SSH Connection (with logs)
        run: |
          echo "Attempting SSH connection to $SSH_USER@$SSH_HOST"
          sshpass -p "$SSH_PASSWORD" ssh -vvv -o StrictHostKeyChecking=no "$SSH_USER@$SSH_HOST" "echo ✅ SSH connection successful!"
        env:
          SSH_USER: ${{ secrets.USER }}
          SSH_PASSWORD: ${{ secrets.PASSWORD }}
          SSH_HOST: ${{ secrets.HOST }}
      - name: Deploy to Server
        run: |
          ssh $SSH_USER@$SSH_HOST << 'EOF'
            cd /your/project/path
            git pull origin main
            docker stop my_container || true
            docker rm my_container || true
            docker build -t loneDocker .
            docker run -d --name my_container -p 80:3000 my_app_image
          EOF
